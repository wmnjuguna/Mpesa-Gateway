spring:
  datasource:
    url: jdbc:mariadb://credit-ecosystem.czomqym4ckho.us-east-2.rds.amazonaws.com:3306/payments
    username: admin
    password: ddDXMmUVDDLpyF1vlZ8D
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update # or create-drop, create, none
      properties:
        hibernate:
          jdbc:
            batch_size: 50 # set batch size for batch inserts/updates

  rabbitmq:
    host: 172.31.24.62
    port: 5672
    username: admin
    password: sudoConnect
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 3s
          max-attempts: 2
          max-interval: 10s
          multiplier: 2